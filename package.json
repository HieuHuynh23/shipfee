{
  "name": "shipfee-root",
  "version": "1.1.0",
  "description": "Root package for Vercel deployment compatibility",
  "scripts": {
    "build": "mkdir -p public && cp -r customer-app shipper-app admin-app public/"
  }
}
