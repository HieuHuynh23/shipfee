{
  "name": "shipfee-root",
  "version": "1.1.0",
  "description": "Root package to forward install and start commands to the server subdirectory on Render",
  "scripts": {
    "postinstall": "cd server && npm install",
    "build": "cd server && npm install",
    "start": "cd server && npm start"
  }
}
